config {
  type: "operations",
  hasOutput: true,
  name: "raw_orders"
}

CREATE OR REPLACE EXTERNAL TABLE `${dataform.projectConfig.defaultDatabase}.${dataform.projectConfig.defaultSchema}.raw_orders` (
  id INT64,
  user_id INT64,
  order_date DATE,
  status STRING
)
OPTIONS (
  format = "CSV",
  uris = ["gs://jaffleshop-483809/csv/raw_orders.csv"],
  skip_leading_rows = 1
);
