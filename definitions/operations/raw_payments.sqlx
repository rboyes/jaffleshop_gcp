config {
  type: "operations",
  hasOutput: true,
  name: "raw_payments"
}

CREATE OR REPLACE EXTERNAL TABLE `${dataform.projectConfig.defaultDatabase}.${dataform.projectConfig.defaultSchema}.raw_payments` (
  id INT64,
  order_id INT64,
  payment_method STRING,
  amount INT64
)
OPTIONS (
  format = "CSV",
  uris = ["gs://jaffleshop-483809/csv/raw_payments.csv"],
  skip_leading_rows = 1
);
