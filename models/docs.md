{% docs orders_status %}
Order status values: placed (placed, not shipped), shipped (in transit), completed (delivered),
return_pending (return requested, not received), returned (return received).
{% enddocs %}
